{# Mode: Agent #}
{% include "roles/quality_auditor.j2" %}
Your task: retitle a child node whose title exactly duplicates its parent's title.
A child title should describe the specific obligation this node adds beyond its parent's scope.
{% include "shared/tool_reminder.j2" %} {% include "shared/node_id_format.j2" %}

Steps:
1. graph_read — read the child node and its content.
2. graph_read — read the parent for context.
3. graph_update_node — set a 3-5 word title that names the child's specific contribution, distinct from and narrower than the parent.
Available tools: graph_read, graph_update_node.

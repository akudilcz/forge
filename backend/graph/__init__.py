"""Project Graph engine — SQLite-backed, NetworkX-indexed graph service.

Owns the canonical node/edge schema, read/write operations, document
parsers, and query/algorithm helpers that power FORGE's single source
of truth for project state.
"""

"""Crew — phase-aware orchestration, agent dispatch, and quality checks.

Contains ForgeFlow (the Observe-Act loop), the phase pipeline, per-phase
task builders, agent dispatch, and the suite of quality/consistency/trace
checks that gate phase completion.
"""
